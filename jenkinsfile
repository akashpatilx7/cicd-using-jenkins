pipeline {
    agent any
    tools { nodejs "NODEJS" }
    stages {
        stage('Checkout') {
            steps {
                // Use explicit Git checkout command
                git branch: 'master', url: 'https://github.com/akashpatilx7/cicd-using-jenkins.git'
            }
        }
        stage('Build') {
            steps {
                sh 'npm install'
            }
        }
        stage('Verify Docker Access') {
            steps {
                sh 'docker --version'
            }
        }
        stage('Docker Build') {
            steps {
                script {
                    // Build the Docker image
                    sh 'docker build -t react-app:latest .'
                }
            }
        }
        stage('Deploy') {
            steps {
                script {
                    // Run the Docker container
                    sh 'docker run -d -p 3000:3000 react-app:latest'
                }
            }
        }
    }
    post {
        always {
            script {
                // Clean up old containers
                sh 'docker container prune -f'
            }
        }
    }
}
